spring.application.name=RMJHallAdmin
spring.security.user.name = mercy
spring.security.user.password = iamthatiam@28

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Railway in production, localhost in development
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/RMJHALL}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:12345}

# ORM / Hibernate
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect